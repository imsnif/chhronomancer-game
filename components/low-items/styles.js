import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  itemContainer: {
    flex: 0,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    margin: 5
  },
  itemStyle: {
    height: 30,
    width: 30
  }
})
